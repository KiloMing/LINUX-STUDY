# 2026-09-07 学习记录

## 今日目标

今天主要时间投入嵌入式工程，Linux 学习时间较少；简要回顾 condition variable，并开始 pthread rwlock。当前统一使用 C++ + pthread。

## 闭卷回顾结果

已回顾 `ready=true` 只改变条件，`pthread_cond_signal()` 通知并唤醒等待线程（存在等待者时）；被唤醒后仍需重新获得 mutex，`pthread_cond_wait()` 才返回。知道 signal 唤醒至少一个等待线程、broadcast 唤醒所有等待线程的区别。

在 `if` vs `while` / 虚假唤醒变式中答错一次，仍需复测，不能记为已掌握。本次依据学习者提供的回顾结果记录，未新增完整闭卷测试通过的证据。

## 今日真正掌握

本次没有足以确认新增掌握等级的独立证据。rwlock 入门理解：读+读可并发、读+写不可并发、写+写不可并发；认识 `pthread_rwlock_t`、`pthread_rwlock_init`、`pthread_rwlock_rdlock`、`pthread_rwlock_wrlock`、`pthread_rwlock_unlock`、`pthread_rwlock_destroy`，以及 `pthread_create` / `pthread_join`。仍为学习中/待验证。

## 做过的代码或实验

尚未完成 2 Reader + 1 Writer 独立实现；本次没有新增独立代码或运行结果，不升级到 L3，不修改 `MASTERY.md`。

## 暴露的问题

`while` 重检条件与虚假唤醒变式需要复测；一次答错不直接判定为长期知识缺口。

## 下一次测试

1. 先不看笔记复测 condition variable 的 `while` / 虚假唤醒变式，解释 wait 的状态变化并写出 mutex + while 同步骨架。
2. 再不看答案，用 C++ + pthread 独立实现、编译运行 2 Reader + 1 Writer rwlock 最小程序。
3. 通过后再进入 producer-consumer；未通过则回补并复测。

## 证据与参考资料

- 学习者提供的 2026-09-07 回顾与答题情况；不替代独立代码证据。
- 仓库规则：[TEACHING_PROTOCOL.md](../TEACHING_PROTOCOL.md)、[CURRENT.md](../CURRENT.md)、[MASTERY.md](../MASTERY.md)、[daily/README.md](README.md)。
