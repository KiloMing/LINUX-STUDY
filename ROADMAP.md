# ROADMAP

长期目标：形成 Linux/C++ 系统、嵌入式与机器人系统集成能力，能独立实现和调试移动机器人，也为嵌入式 Linux、工业控制、设备端 C++ 保留转向空间。SLAM/Nav2 是系统中的一环，不单押算法岗。

路线按前置关系排列。阶段可以因真实项目、掌握证据或技术变化调整，但不能只因为“想跳过”而跳过。

## 时间与安排

主线：Linux/C++ 系统能力 → Socket/Linux 调试 → CMake/C++ 工程化 → ROS2 → TF2/URDF/RViz/仿真 → 运动学/Odometry → STM32+ROS2 底盘/ros2_control → LiDAR/IMU/状态估计 → SLAM/Nav2 → 综合机器人项目。

按每周 8–12 小时安排，下表工时以每周约 10 小时估算，共约 460–610 小时（46–61 个常规学习周，约 11–14 个月）。考试、返工和硬件等待会延长日历时间；时间用来安排投入，验收通过才进入下一阶段。

| 阶段 | 预计周数 | 参考投入 |
|---|---:|---:|
| 1. Linux 并发收尾 | 1–2 周 | 10–20 小时 |
| 2. Socket/Linux 调试 | 3–4 周 | 30–40 小时 |
| 3. CMake/C++ 工程化 | 3–4 周 | 30–40 小时 |
| 4. ROS2 | 5–6 周 | 50–60 小时 |
| 5. TF2/URDF/RViz/仿真 | 4–5 周 | 40–50 小时 |
| 6. 运动学/Odometry | 5–6 周 | 50–60 小时 |
| 7. STM32+ROS2 底盘/ros2_control | 6–8 周 | 60–80 小时 |
| 8. LiDAR/IMU/状态估计 | 5–6 周 | 50–60 小时 |
| 9. SLAM/Nav2 | 6–8 周 | 60–80 小时 |
| 10. 综合机器人项目 | 8–12 周 | 80–120 小时 |

弹性课表（时段可以换，不补欠课）：

- 普通周：两次各 2 小时学模型和写最小练习，一次 3–5 小时做项目/排错，另留 1–3 小时闭卷复测、整理证据，共 8–12 小时。
- 考试周：降到约 5 小时，2 小时复现旧题、2 小时完成一个小变式、1 小时记录和复盘；新内容顺延，不降低验收标准。
- 每次先做 5–10 分钟闭卷回顾；每周只定一个可验收的小结果，未通过就缩小任务继续练。

按约 12 周滚动看投入：70% 通用核心 / 20% 机器人 / 10% 前沿探索。通用核心包括 C/C++、Linux、调试、通信、构建、嵌入式接口与测试；机器人部分练 ROS、坐标、运动模型和导航。机器人项目中的通信与调试也计入通用核心，同一小时不重复计算。前沿探索只做阅读或小实验，不挤占前置验证，不提前开 VLA/强化学习等长期支线。

每约 12 周做一次职业/行业校准：查看目标地区的机器人系统、嵌入式 Linux、设备端 C++ 等岗位要求，记录样本日期、反复出现的技能和自己的项目证据；结合可查的产品落地信息调整下一周期侧重。结论写入当日 daily，需要改路线时同步 CURRENT 和本文件，不因单条热点跳过基础。

## 阶段 1：Linux 系统编程收尾

**状态：当前**

**核心任务：** 先做 3 Producer + 2 Consumer 变式，再闭卷从空文件复现有界队列与结束协议；之后独立完成 2 Reader + 1 Writer，增加第二个 Writer 验证写写互斥。保留代码、输出和一次阻塞排查记录。

**学习范围：** condition variable、read-write lock、生产者消费者、线程同步综合练习，以及对已有 IPC/同步代码的调试。

**完成证据：** 不看答案独立完成一个线程同步程序；修改线程数量或共享条件仍能正确工作；能够定位死锁、遗漏解锁、虚假唤醒处理等常见问题；完成一个面向后续通信的并发小程序。

**下一阶段门槛：** condition variable 与 rwlock 至少达到 L3，能够解释锁保护的条件和线程状态变化。

> 高性能线程池、Reactor、大规模并发服务器和 epoll 深度优化不属于当前机器人关键路径。需要时再补。

## 阶段 2：Socket 与 Linux 调试

**状态：未开始**

**核心任务：** 先写 TCP client/server，处理短读写、消息边界与断连，再做 UDP；用 GDB、strace、ss 和日志排查阻塞、端口和连接问题，完成模拟机器人遥测程序。

**学习范围：** IP、port、TCP、UDP、client/server，`socket`、`bind`、`listen`、`accept`、`connect`、`send/recv`、`sendto/recvfrom`、关闭与错误处理。

**完成证据：** 独立实现 TCP 与 UDP 最小通信；修改消息格式和通信方向；定位端口占用、断连、短读写等问题；完成一个模拟机器人遥测或控制通信程序。

**下一阶段门槛：** 能说明 TCP/UDP 的取舍，并为一个机器人通信场景选择和验证方案。

## 阶段 3：CMake 与 C++ 工程化

**状态：未开始**

**核心任务：** 把遥测程序整理为多目录 C++ 工程，拆出通信库和测试目标；练习 RAII、对象生命周期、智能指针、STL 和错误处理，用编译警告及 sanitizer 检查资源和内存问题。

**学习范围：** target、可执行文件、静态/动态库、include 路径、链接、`find_package`、多目录工程、构建目录分离。

**完成证据：** 从空目录建立多 target C++ 工程；新增库和测试目标；定位链接或 include 错误；用 CMake 重构一个已有练习但不改变其行为。

**下一阶段门槛：** 能在不复制模板答案的情况下组织一个可重复构建的 C++ 项目。

## 阶段 4：ROS 2 基础与调试

**状态：未开始**

**核心任务：** 建立 C++ package，依次练 topic、service、action、parameter 和 launch；串起模拟传感器与控制节点，用 CLI、日志和 rosbag 观察及回放数据，排查一次 QoS 或参数问题。

**学习范围：** workspace、package、node、topic、message、service、action、parameter、launch，以及 `ros2 node/topic/service/action/param` 调试命令。

**完成证据：** 独立创建 publisher/subscriber 和 service/action 示例；改变消息与参数；定位节点、话题、QoS 或参数问题；完成一个传感器数据到控制命令的小型数据流。

**下一阶段门槛：** 能说清每个节点的输入、输出、参数、依赖和失败模式。

## 阶段 5：TF2、URDF、RViz 与仿真

**状态：未开始**

**核心任务：** 建立差速机器人 URDF 和 TF 树，在 RViz 与仿真中显示模型及传感器；修改安装位姿，并排查一次错误 frame 或时间戳。

**学习范围：** 坐标变换、旋转表示、`map/odom/base_link/sensor` 帧、URDF、robot_state_publisher、RViz 和仿真。

**完成证据：** 独立建立机器人模型与 TF 树；改变传感器位姿；故意制造错误 frame 或变换并定位；在 RViz/仿真中正确显示模型与传感器。

**下一阶段门槛：** 能解释每条 TF 的发布者、父子关系、时间戳和用途。

## 阶段 6：运动学与 Odometry

**状态：未开始**

**核心任务：** 从轮径、轮距、编码器增量计算速度和位姿，发布 odom/TF；用直行、原地旋转、圆弧和零速度输入验证方向、单位与积分。

**学习范围：** 差速/实际底盘运动模型、轮速到机体速度、离散积分、里程计消息和协方差基础。

**完成证据：** 独立推导并实现运动模型；修改轮距或轮径参数；定位方向、单位或积分错误；先在仿真/离线数据中验证轨迹。

**下一阶段门槛：** 模型输出能够与已知输入和边界案例相符，并能发布一致的 odom 与 TF。

## 阶段 7：STM32 + ROS2 底盘与 ros2_control

**状态：未开始**

**核心任务：** 完成编码器读取、电机速度闭环及串口/CAN 协议（选一种）；接入 ROS2 和 ros2_control 命令/状态接口，测试校验、超时停止、限速和失联恢复。

**学习范围：** 主机与 MCU 通信、编码器、电机驱动、速度闭环、命令与状态接口、ros2_control 硬件抽象。

**完成证据：** 完成命令下发和状态回传；改变通信频率或控制目标；定位丢包、符号、单位、超时或控制问题；使真实底盘按安全测试流程运动。

**下一阶段门槛：** 真机速度与里程计数据稳定、可解释、可记录，并具备失联停止等安全行为。

## 阶段 8：LiDAR、IMU 与状态估计

**状态：未开始**

**核心任务：** 接入 LiDAR 和 IMU，检查频率、时间、坐标、单位与标定；录制轮式里程计和 IMU 数据，比较融合前后的轨迹及漂移，记录异常输入处理。

**学习范围：** 驱动、消息、时间戳、frame_id、标定、噪声、IMU/轮式里程计融合、LiDAR 数据检查。

**完成证据：** 独立接入传感器；修改频率或坐标配置；定位时间戳、帧、单位、漂移或异常值；在 RViz 和记录数据中验证输入质量。

**下一阶段门槛：** `/scan`、IMU、odom 与 TF 在时间和坐标上保持一致，基础状态估计可重复运行。

## 阶段 9：SLAM、定位与 Nav2

**状态：未开始**

**核心任务：** 先仿真再真机完成建图、保存地图、重定位与导航；对比参数实验，记录到达结果和失败原因，沿 scan → TF/odom → 定位 → costmap → 规划/控制 → cmd_vel 排查。

**学习范围：** 建图输入输出、scan matching 基础、地图、定位、代价地图、规划、控制、恢复行为与系统调试。

**完成证据：** 在仿真和真机分别完成建图/定位/导航；改变地图或参数；定位 TF、传感器、定位或规划失败；保存一套带实验记录的可复现配置。

**下一阶段门槛：** 能从传感器输入一路解释到速度命令，并能用证据定位主要失败环节。

## 阶段 10：综合机器人项目

**状态：未开始**

**核心任务：** 整合底盘、传感器、定位和导航，完成指定路线巡航与遥测；整理启动步骤、配置、接口、日志、测试数据和演示。测试障碍物、传感器断流、通信中断及重新启动。

**学习范围：** 系统集成、接口约定、故障处理、可重复部署、性能记录与项目复盘。视觉、语音、机械臂等扩展按实际需求另选，不作为本阶段必修。

**完成证据：** 从干净工作空间按 README 完成构建和启动；至少重复完成三次同一路线任务，记录成功率、耗时、定位误差的测量方法及失败原因；完成断流/失联测试并留下诊断和恢复记录。

**验收门槛：** 项目开始时约定路线、误差和停止响应时间等指标，结束时逐项对照；能独立解释整条数据流和关键取舍，隔一段时间仍能复现部署及故障定位。保留一份可单独运行的通信/设备模块，作为通用工程能力证据。
