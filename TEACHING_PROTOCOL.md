# TEACHING_PROTOCOL

目标不是让学习过程显得顺利，而是最终能够在没有逐行提示时，独立设计、实现、验证和调试机器人系统。

## 1. 真实性高于附和

- 学习者的自信、重复坚持或期待不能作为正确证据。
- 路线或理解错误时直接指出，并给出可检查的理由。
- 不知道就标记未知并查证，不为了继续讲课而猜测。
- 这套协议只管理学习流程；仓库和学习目标的最终决定权属于学习者。

## 2. 证据优先级

Linux/POSIX：标准、manual page、系统实现文档 → 权威教材 → 成熟工程 → 论坛/博客。

CMake/ROS/Nav2：官方文档、规范、迁移指南和源码 → 官方维护者资料 → 成熟工程 → 博客/视频。

嵌入式：datasheet、reference manual、厂商 SDK/application note → 官方例程 → 成熟工程 → 博客。

SLAM：原始论文、权威教材/survey → 官方或论文配套实现 → 其他解释。

低等级来源可以帮助理解，但不能覆盖高等级来源。版本敏感结论必须同时核对 [ENVIRONMENT.md](ENVIRONMENT.md)。

## 3. 每次学习开始前

1. 读取 [CURRENT.md](CURRENT.md)。
2. 查看最近一次 daily 记录。
3. 不看答案完成 5–10 分钟的“下一次测试”。
4. 根据结果选择继续、回补或重新测试。

一次失败不直接写成知识缺口。先去掉提示、换一个变式，并在稍后再次测试，区分概念错误、操作不熟、提示依赖和粗心。

## 4. 固定学习闭环

```text
建立理论模型
  ↓
完整 worked example
  ↓
逐步撤掉提示（fade）
  ↓
脱离答案独立复现
  ↓
修改需求形成变式
  ↓
故意制造并定位错误
  ↓
用于一个实际任务
  ↓
隔天或间隔后再次提取
```

随着能力提高，完整示例和提示要减少，设计、复现和评审责任逐步交给学习者。

## 5. Definition of Done

一个章节只有同时满足以下条件才算完成：

- 能用自己的话解释模型和执行流程。
- 能指出输入、输出、依赖、配置和主要失败模式。
- 能从空文件或空工程独立写出最小实现。
- 改变需求后仍能修改实现。
- 至少定位并修复一个故意或真实问题。
- 有一个与机器人路线有关的实践出口。
- 延迟后仍能完成关键复现。

“看完”“听懂”“复制后跑通”都不能单独算完成。

## 6. 反黑盒

运行一个命令或 launch 文件时，至少回答：启动了什么、数据从哪里来、到哪里去、参数在哪里、依赖什么、失败时如何观察。对 SLAM/Nav2 还要能沿 `/scan`、odom、TF、map 和 `/cmd_vel` 追踪数据流。

反黑盒不等于从头重写所有库。顺序是：会使用 → 理解系统 → 会配置 → 会调试 → 理解核心算法 → 有项目需要时深入实现。

## 7. 实践与仿真

- 每个知识点尽快对应可运行代码、可观察实验或硬件行为。
- 先用仿真或离线数据验证模型和接口，再进入真机。
- 仿真是验证工具，不是最终成果；硬件阶段必须重新检查噪声、时序、单位和安全行为。

## 8. 路线控制

必要前置不能因偏好跳过，非核心内容也不能无限深入。路线只在以下情况调整：

1. 前置知识已有可靠掌握证据；
2. 真实项目出现新需求；
3. 当前路线存在技术错误；
4. 软件、硬件或生态发生变化；
5. 某段内容投入产出比过低；
6. 测试暴露了需要回补的基础。

路线变化要同步更新 [ROADMAP.md](ROADMAP.md) 和 [CURRENT.md](CURRENT.md)，并说明依据。

## 9. 记录规则

- `ROADMAP.md` 只存长期路线和阶段门槛。
- `CURRENT.md` 只存当前事实、下一次测试和下一步。
- `MASTERY.md` 只按证据更新等级。
- `daily/` 记录当天真正掌握、错误、实验和待测试内容，不追求长篇复述教材。
- 代码、命令输出和实验结果优先于“我感觉已经会了”。
